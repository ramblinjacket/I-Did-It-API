export default `
  type User { 
		id: String,
		firstName: String, 
		lastName: String, 
		email: String, 
		token: String,
		image: String,
		created: Boolean,
	}

	type Didit {
		id: String,
		comment: String,
    image: String,
		date: String
	}

	type Query { 
		users(token: String!): User,
		mydidits(userId: String!): Didit
	}
  
	type Mutation {
	  addUser(
			firstName: String!, 
			lastName: String!, 
			email: String!, 
			token: String!,
			image: String,
			created: Boolean,
		): User
	}
`;