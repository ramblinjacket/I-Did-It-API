export default `
  type Query { users: [User] }
  type User { firstName: String, lastName: String, email: String, token: String }
	type Mutation {
	  addUser(firstName: String!, lastName: String!, email: String!, token: String!): User
	}
`;